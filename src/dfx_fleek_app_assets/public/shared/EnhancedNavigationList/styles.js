import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  wrapper: {
    position: 'sticky',
    top: 0,
    alignSelf: 'flex-start',
    flexBasis: 212,
    flexShrink: 0,
    marginRight: 21,
  },
}));
