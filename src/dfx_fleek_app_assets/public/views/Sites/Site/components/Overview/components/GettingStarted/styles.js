import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  list: {
    margin: 0,
    padding: 0,
    listStyleType: 'none',
  },
  sectionWrapper: {
    marginBottom: 16,
  },
});
