import { makeStyles } from '@material-ui/core';

export default makeStyles({
  accountHeaderCard: {
    marginTop: 22,
  },
  accountEmail: {
    margin: '12px 0',
  },
  skeletonRoot: {
    borderRadius: 4,
  },
});
